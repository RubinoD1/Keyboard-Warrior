To-DO 
-------
#3 Make arrays for quotes 
  - import/export 
#6 Settings: 
  - Normal / Hard modes (qoute lengths)
  - themes?
#7 Make site header a link to homepage
  - test if this resets all the defaults (properly hidden divs, etc)
#8 Game completion screen
  - total accuracy values into a total 
  - replay btn, returns to theme select screen (cards).

Today 

- ONCE last input position = qoute length end qoute 
  - Right now it needs another input to switch to next 

- End game working 
 
- when qoute complete start next 
  - push accuracy value to array to calculate total overall %
  - increase index position by 1 
    - if at the end of array - start results screen, have replay btn 

- calculate accuracy % 
  - accuracy % html needs to reset to --% at the beginning of each qoute. 
    **DOUBLE CHECK IF ACCURATE SCORE IS GIVEN**
  - when qoute complete save score to array for total % at the end of the game. 
  
- Hide site header when game starts
  - unhide on the results screen.




NOTES
--------

- Progress bar for quotes left: 
  - will need to manipulate the value of progress bar to update progress (JS). 
  - remeber to set default value to 0 in the HTML file when testing is done. 

- Qoute current location marker
 - Increase the font size, make character bold and have a border around location (black spaces will need to be marked somehow). 


JS NOTES 
-------------
//Qoute logs for refrence 
//console.log(quote.textContent); //quote text content
//console.log(quote.textContent.charAt(0)); //get specefic character
//console.log(quote.textContent.length); //qoute length
-------------- 


Keyboard info- logs relevant info 
----

  const keyCodes = () => {
  document.addEventListener('keydown', function (e) {
    console.log(
      'keyCodeDEP', e.which,
      'key', e.key,
      'code', e.code,
      'location', e.location
    );
  });
};
keyCodes();

----------
